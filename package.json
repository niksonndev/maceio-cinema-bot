{
  "name": "maceio-cinema-bot",
  "version": "1.0.0",
  "description": "Bot de programação de cinemas em Maceió (Ingresso.com) para Telegram",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "bot:listen": "tsx src/bot.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "bash scripts/test.sh",
    "test:docker": "docker compose -f docker-compose.test.yml run --rm --build test",
    "test:watch": "docker compose -f docker-compose.test.yml run --rm test sh -c \"npm ci && npx vitest\"",
    "lint": "eslint src/ test/",
    "lint:fix": "eslint src/ test/ --fix",
    "format": "prettier --write src/ test/",
    "format:check": "prettier --check src/ test/",
    "sam:build": "npm run build && sam build",
    "sam:deploy": "sam deploy",
    "sam:warm": "bash scripts/sam-warm.sh",
    "sam:local": "bash scripts/sam-local.sh"
  },
  "keywords": [
    "cinema",
    "ingresso",
    "telegram",
    "bot"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist/**/*",
    "package.json",
    "package-lock.json"
  ],
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1114.0",
    "axios": "^1.6.0",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.67.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^4.17.23",
    "@types/node": "^24.5.2",
    "@types/node-telegram-bot-api": "^0.64.11",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.4.0",
    "prettier": "^3.8.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vitest": "^3.2.7"
  }
}
